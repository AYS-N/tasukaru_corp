# デザイン刷新（2026-09-12）

トップを白背景、自然光のカラー写真、濃い文字に変更し、整理・リユースの温かみを伝える構成にしています。ロゴは刷新前の公開サイトの緑色「タスカル／tasukaru」に戻しています。

- 冒頭の梱包と最後のカメラ査定を、明るい室内の手元写真に改訂。PCでは白いコピー領域と写真を左右に、スマートフォンでは上下に配置しています。
- 中央の5枚の作業写真は配置・サイズ・横移動を維持。白背景に元のカラーと明るさで表示します。顔が写らない構図と事業一覧写真との使い分けを継続しています。
- スクロールに連動した横移動、最後の場面での段階的なコピー表示、演出終了後のヘッダー表示を維持。動きを減らす設定や小さい横向き画面では通常の縦表示を使用します。
- ロゴはヘッダー・フッター・代表挨拶ページの装飾に反映し、英字部分が切れないよう調整しています。
- 黒いヘッダーとフッター、Zen Kaku Gothic New / Manrope、6事業それぞれの写真を継続。サービス写真に暗くする加工はありません。
- 会社情報・住所・電話番号・下層本文は維持。事業一覧バナーは承認済みの「整理から買取・再販売まで、一貫して対応」です。
- ナビゲーションには「トップ」を含みます。

## 編集対象

配信する静的HTMLはリポジトリのルートにあります。共通デザインは `design.css` / `refinement.css`、トップは `hero-scroll.css` / `hero-scroll.js` です。`project/` は以前の書き出しデータです。

今回の写真は `assets/hero-care-daylight-v2.png` と `assets/hero-appraisal-daylight-v2.png`。生成時の編集指示は `docs/daylight-image-prompts.md` に記録しています。写真はAIで作成したイメージで、実在の社員・施工実績を示すものではありません。使用ロゴは既存の `assets/logo-tasukaru-green.png` です。

## 公開用の確認

16ページの内部ファイル参照、会社情報・下層本文、JavaScript構文を検証しています。ブラウザーでPC・スマートフォンのトップ表示と横移動、画像の読み込みを確認しています。

プレビュー用バッジ・検索除外タグ（404を除く）・フォームの疑似確認処理は公開版にはありません。既存の `contact-send.php` へのPOST、項目名、種別切替、成功・エラー表示を維持。PHP・SMTP設定は `mail-setup.md` を参照してください。今回メールの実送信は行っていません。

Googleマップの設定と座標は元のままです。アプリ内ブラウザーで空白になる現象は元の公開サイトでも確認されており、通常ブラウザーでの確認が残っています。元から存在する仮メールアドレス等も変更していません。
